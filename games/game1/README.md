# Walkthrough:

* Terms:
    - a,b are complemantary if a + b = k + 1
    - offset is s % (k+1)

* With Repetitions:
    - if s == (k+1)*n, where n is some integer -> P2 always wins
    - Else, P1 always wins
* W/O Repetitions:
    - if s == (k+1)*n, where n is some integer -> P2 always wins
    - 